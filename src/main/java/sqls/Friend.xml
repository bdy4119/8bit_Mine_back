<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mul.cam.a.dao.FriendDao">

<insert id="acceptfriend" parameterType="mul.cam.a.dto.FriendDto">
	insert into friend(id, friendid)
	values(#{id}, #{friendid})
</insert>

<select id="friendlist" parameterType="String" resultType="mul.cam.a.dto.FriendDto">
	select * from friend
	where id=#{id}
</select>

<delete id="deletefriend" parameterType="Integer">
	delete from friend
	where seq=#{seq}
</delete>

</mapper>